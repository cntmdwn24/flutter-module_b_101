import 'package:flutter/material.dart';

class HdstRepository extends StatelessWidget {
  const HdstRepository({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
